To build anything, you need to fetch the vcglib submodule with git submodule init && git submodule update

-- Compiling stltools
To compile, create a folder outside the git folder, change to it, and run 
ccmake ../meshcleaner
make

You can also load the project into Qt Creator
